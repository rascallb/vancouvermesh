https://github.com/rascallb/vancouvermeshbaseURL: https://vancouvermesh.ca/
languageCode: en-us
title: vancouvermesh.ca
theme: ["re-terminal"]
pagination.pagerSize: 5
caches:
  images:
    dir: :cacheDir/images

params:
  # dir name of your blog content (default is `content/posts`)
  contentTypeName: about
  # ["orange", "blue", "red", "green", "pink"]
  themeColor: green
  # if you set this to 0, only submenu trigger will be visible
  showMenuItems: 3
  # show selector to switch language
  showLanguageSelector: false
  # set theme to full screen width
  fullWidthTheme: true
  # center theme with default width
  centerTheme: false

languages:
  en:
    languageName: English
    title: vancouvermesh.ca
    subtitle: Building a solar-powered text messaging network in Vancouver, BC
    keywords: 'meshtastic, heltec, vancouver, yvr'
    menuMore: Show more
    readMore: Read more
    readOtherPosts: Read other posts

    params:
      logo:
        logoText: Vancouver MESH
        logoHomeLink: /

    menu:
      main:
      - identifier: about
        name: About
        url: /about
      - identifier: join
        name: Join
        url: /join
      - identifier: connect
        name: Connect
        url: /connect
      - identifier: learn
        name: Learn
        url: /learn